// Array indexing starts with "1", using loop iterate an array.

arr = [0, 1, 1, 2, 3, 5, 8]
for i in (1, len(fib)): // it is nothing but i =1 and ends with array length.

    print(fib[i]) 


Output:

    0 1 1 2 3 5 8